package app.cash.sqldelight.db

/**
 * Stub para permitir compilação no target WASM sem dependência real do SQLDelight.
 * Como não usamos persistência no WASM, esta interface vazia satisfaz o `expect`.
 */
interface SqlDriver
